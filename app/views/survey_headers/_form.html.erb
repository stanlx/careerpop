<%= form_for(@survey_header) do |f| %>
  <% if @survey_header.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@survey_header.errors.count, "error") %> prohibited this survey_header from being saved:</h2>

      <ul>
      <% @survey_header.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :survey_name %><br>
    <%= f.text_field :survey_name %>
  </div>
  <div class="field">
    <%= f.label :instructions %><br>
    <%= f.text_field :instructions %>
  </div>
  <div class="field">
    <%= f.label :other_header_info %><br>
    <%= f.text_field :other_header_info %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
